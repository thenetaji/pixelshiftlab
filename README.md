# PixelShiftLab :- An all in one file processing tool which can spin up in just a few minutes.

# Installation:-
  - Clone the repo
    ```bash 
    "git clone https://github.com/thenetaji/pixeoshiftlab.git"
  - Change dir and Install dependencies in both frontend and backend
    ```bash
    "cd pixelshiftlab && cd frontend && npm install && cd ../backend && npm install"
    
  Thanks!!